let emptyArray = [];

function findDivCount(a, b, c) {
    let newArray;
    for (let i = a; i <= b; i++) {
        if (i % c === 0) {

            newArray = emptyArray.push('i');

        }
    }
    console.log(emptyArray.length);
}