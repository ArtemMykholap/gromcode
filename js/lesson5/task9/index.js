let emptyArray = [];

function findDivCount(a, b, n) {
    let newArray;
    for (let i = a; i <= b; i++) {
        if (i % n === 0) {

            newArray = emptyArray.push('i');

        }
    }
    console.log(emptyArray.length);
}