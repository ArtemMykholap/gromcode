const names = ['John', 'Olivya', 'Oleksandr', 'Emanuel', 'Vanya', 'Nastya'];
const text = 'ya';
let arr = [];

names.filter(function(names, text) {
    if (names.length > 5)
        if (names == ) {
            arr++;
        }
    console.log(arr);
});