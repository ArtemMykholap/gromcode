const names = ['John', 'Olivya', 'Oleksandr', 'Emanuel', 'Vanya', 'Nastya'];
const text = 'ya';
let arr = [];

const filterNames = (names, text) = names.filter()
if (names.length > 5)
    if (names === text) {
        arr++;
    }
console.log(arr);;